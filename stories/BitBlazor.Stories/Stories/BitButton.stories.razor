@attribute [Stories(nameof(BitButton))]

<Stories TComponent="BitButton">
    <ArgType For="_ => _.Color" Control="ControlType.Color" />

    <Story Name="Default">

        <Arguments>
            <Arg For="_ => _.Text" Value="@("Default")" />
            <Arg For="_ => _.Color" Value="Color.Primary" />
        </Arguments>

        <Template>
            <BitButton @attributes="context.Args" />
        </Template>

    </Story>
</Stories>
